# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# iCloud
.com.apple.timemachine.donotpresent
.fseventsd
.Spotlight-V100
.Trashes

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code session state
.claude/

# Node (only if some future project uses it)
node_modules/
npm-debug.log*
yarn-debug.log*

# Python (only if some future project uses it)
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/

# Logs & temp
*.log
tmp/
.tmp/

# Raw working assets that don't belong in the repo
# (kept locally if needed, re-derive `cards/*.jpg` from them as new sets arrive)
**/raw-photos/
**/_intermediate/
**/cards-original/
# Rulebooks — IP belongs to Renegade / Dire Wolf, kept locally for reference
**/rulebooks/
*.HEIC
IMG_*_full.jpg
IMG_*_r*c*.jpg
